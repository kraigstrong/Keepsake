# Dependencies
node_modules/
.pnp
.pnp.js

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native (generated by prebuild — regenerate, don't hand-edit or commit unless explicitly decided otherwise)
# Anchored to repo root: unanchored ios/ or android/ would also swallow
# local Expo modules' own native source under modules/*/ios|android/.
/ios/
/android/
.kotlin/
.metro-health-check*
*.orig.*

# EAS
.eas/

# Env / secrets — generate locally from 1Password, never commit
.env
.env.local
.env.*.local
# scripts/prepare-archive-env.mjs parks your development .env.local here
# while an archive-time snapshot is in place. Holds real credentials.
.env.local.bak
.env.example
client.env
server.env
devtools.env
*.pem
*.p8
*.p12
*.mobileprovision
*.keystore
*.jks
*.key

# Supabase local
supabase/.branches/
supabase/.temp/

# Testing
coverage/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# TypeScript
*.tsbuildinfo

# Build output
build/

# Stray file from initial repo-init troubleshooting session (undeletable on this
# sandbox's mount — harmless, ignored rather than fought)
/testfile.txt
