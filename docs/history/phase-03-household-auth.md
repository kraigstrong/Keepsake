# Phase 3 — Authentication, Household, and Security Boundary

**Result:** Conditional Pass | **Date:** 2026-08-02 | **PR:** [#15](https://github.com/kraigstrong/Keepsake/pull/15)

18 commits on `phase-3-household-auth`, all 4 CI checks green. ADR-0008: email OTP auth over passwords (non-technical audience, no password to recover); SECURITY DEFINER Postgres RPCs over Edge Functions for household/invitation writes (atomic, no external I/O). Schema + RLS for `profiles`/`households`/`household_membership`/`invitations`, each with an explicit `GRANT` (Phase 0's "permission denied" lesson applied again) and no direct client insert/update/delete on households/membership/invitations — every write goes through `create_household`/`create_invitation`/`accept_invitation`, each SECURITY DEFINER and each with pgTAP coverage (cross-household isolation, expiry, replay, idempotent same-caller re-acceptance, abuse controls: 5 live invitations per household + 30s cooldown). Real Supabase Auth (`supabase-js`) replacing the Phase 2 session stub — `LargeSecureStore` (AES-CTR, key in Keychain, ciphertext in AsyncStorage) works around SecureStore's ~2KB limit, documented as an ADR-0007 amendment; `AppState`-driven `startAutoRefresh`/`stopAutoRefresh` since the token-refresh timer alone isn't reliable backgrounded on mobile. New `HouseholdProvider` + `DeepLinkProvider` + `app/onboarding.tsx` inserted into the route guard as a third `Stack.Protected` branch (profile → household-or-accept-pending-invite); household roster + invite-via-native-Share-sheet added to Settings. `recipe-images` Storage bucket + household-scoped policies (with a `safe_uuid()` helper so a malformed object path degrades to a clean RLS denial instead of a cast error) set up ahead of Phase 4/10's need. `example_widgets` retired now that real tables are the canonical RLS example.

HH-01/02/03, SEC-03/04 → `Done (tested)`. 34 suites, 168 passed; typecheck/lint/format clean; `npm audit --omit=dev --audit-level=high` exit 0.

**Also fixed in passing (surfaced by Phase 3.5's session):** PR #15 hit CI for the first time and failed on two real bugs the migrations had never surfaced before (no Docker in this environment meant they'd never actually run) — an ambiguous `expires_at` column reference in `create_invitation()`'s abuse-control guard (its own `RETURNS TABLE` column name shadowed the table column), and `digest()`/`gen_random_bytes()` not resolving because Supabase installs pgcrypto into the `extensions` schema, not `public`, and the functions' `search_path` was scoped too narrowly. Both fixed directly on `phase-3-household-auth`, pushed, all 4 CI checks now green. Also allowlisted two fake invitation-token test fixtures that tripped gitleaks.

## Conditional Pass follow-ups (tracked until closed)

Phase 3 exited **Conditional Pass** (developer decision, 2026-08-02) — build scope and test coverage were real and complete, but three things couldn't be verified in this sandboxed environment at the time:

1. ~~**Migrations/pgTAP have never actually executed anywhere.**~~ **Resolved 2026-08-02** — first CI run on PR #15 caught two real bugs (see above); second run is fully green.
2. ~~**No Simulator or physical-device pass.**~~ **Substantially addressed 2026-08-02** — Phase 3.5's Simulator verification exercised real sign-in, session, and household-provider code live (not just Jest-mocked), though not a full signed-in-for-real flow (no staging backend to sign in against). A genuine physical-device pass is still open.
3. **No staging Supabase connectivity check.** **Resolved 2026-08-05** — staging never actually had this repo's migrations applied at all (CI only ever runs them against a disposable per-PR instance, never the persistent staging project); confirmed missing when a live physical-device onboarding attempt failed with "could not find the table public.profiles" during Phase 6 validation. New "Keepsake Dev Tools" 1Password Environment (`SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROJECT_REF`, mounted as `devtools.env`) linked the Supabase CLI to staging; all 20 migrations (Phase 0 through Phase 6's tombstone table) applied via `supabase db push`, verified with `supabase migration list` (local and remote history now match exactly). Real email-OTP *delivery* is still separately broken — see `docs/current.md`'s "Staging magic-link email is unusable for real sign-up" carried-forward item — but that's an email-template issue, not a missing-schema one.

**Conditional Pass, not Pass**, because at exit time none of the follow-ups above could be verified in this sandboxed environment — all three are now resolved (as of Phase 6/Phase 3.5's live session).
