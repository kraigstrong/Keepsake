# Roadmap

Milestones organize the meaningful outcomes still ahead — the roadmap layer of `Product Vision → Milestone → Work Item → Execution`. A milestone describes what "done" looks like; it is never handed to an agent as a single execution instruction — each work item under it is planned, delegated, and reviewed individually. See `docs/architecture.md` for how the system works today and `docs/prd.md` for product requirements; this file is what's still left to build toward, not a description of the system itself.

Scope, deliberately: this roadmap stops at getting Keepsake in front of friends and family and learning from that. Release-readiness (App Store submission, production rehearsal) isn't planned here yet — revisit once Preview Learning & Iteration has actually run.

Work items are listed here as short entries — objective and why it matters. Full acceptance criteria and constraints get written when a work item is actually selected for execution, not speculatively for the whole backlog up front.

---

## 1. MVP Validation

**What done looks like:** the six required product journeys (website success, manual/offline, shared household, lifecycle, security, credential validation) are confirmed working against current reality, including at least one physical-device pass, with no known data-loss or household-isolation defect remaining. **Exception, stated explicitly so this milestone doesn't quietly depend on milestone 4:** credential validation's compiled-artifact and build-log scan (source-level and generated-native-project scans are separate and already in scope here) is intentionally deferred to Friends & Family Preview's real device build — see that milestone's backlog for why. This milestone's own bar for credential validation is the source/native-project-level scanning already achievable without one.

**Status:** Refresh done, 2026-08-18. Four of six journeys re-confirmed against current code/CI: (2) offline — OFF-01/02/03/05 individually `Done (tested)`, `record_cooking_event`'s idempotency proves no duplicate event on reconnect; (4) lifecycle — confirmed live in Simulator previously, full Jest + CI pgTAP suite re-confirmed passing; (5) security — the best-covered journey, dedicated pgTAP isolation tests plus per-RPC cross-household-rejection cases green in CI; (6) credential validation — native-project scans (`expo prebuild` + `gitleaks`) clean, compiled-artifact/build-log scan deferred to Friends & Family Preview by design (see this milestone's exception note above). Two remain open pending the live joint session below: (1) website success — Library, Recipe Detail, and servings scaling confirmed on a real device 2026-08-18 (the earlier tap-target finding was a Simulator-automation limitation, not an app bug), but the URL-import leg and everything `phase-17-walkthrough-feedback` (PR #63, 2026-08-17) touched — grocery generation/display, Cooking Mode's unit system and notes/history — are still unwalked live; (3) shared household — needs an actual two-actor session, which pgTAP's single-transaction model can't substitute for.

**Backlog:**
- ~~Refresh the six-journey status against what's actually true now.~~ **Done, 2026-08-18.**
- **Live joint session to close Journeys 1 and 3.** Journey 1 (website success) needs the URL-import leg (real Anthropic call, developer watching) plus a fresh walkthrough of grocery generation, Cooking Mode, export, and note-adding — not just because they were never walked live, but because `phase-17-walkthrough-feedback` (PR #63) landed real changes across exactly that ground on 2026-08-17 that have only been test-covered since, never live-confirmed (found during the refresh, via Codex review). Journey 3 (shared household) needs an actual two-actor walkthrough, which pgTAP's single-transaction model can't substitute for.

---

## 2. Reliability

**What done looks like:** production issues are observable and recoverable without guesswork — you can tell what broke, and the system degrades gracefully instead of losing data or getting stuck.

**Backlog:**
- Add crash reporting.
- ~~Add structured server error logging.~~ **Done, 2026-08-19** (branch `reliability/server-error-logging`) — `supabase/functions/import-recipe/index.ts` now `console.error`s at the failure branches that indicate Keepsake's own pipeline broke (AI extraction, and Storage/DB reads or writes past the point they're expected to succeed), so those are findable in Supabase's function logs without first knowing which job to query for. Deliberately does not log expected/user-driven outcomes (a losing claim race, a malformed URL, an unreachable third-party page, thin page content) — those already surface via the stored job row and response, and logging them as errors would just be noise against genuine breakage. `import-recipe` is Deno-only and has no local test coverage (per the file's own header comment); this is a design decision documented in a new comment on the `fail()` helper, not something a test asserts.
- ~~Define retry behavior for recipe import failures.~~ **Partially done, 2026-08-19** (branch `reliability/import-retry-behavior`) — the outbox now automatically retries an import that never got a confirmed response from the Edge Function (`ImportTransportError`, `src/import/api.ts`/`src/import/outboxEngine.ts`), the same treatment already given to the household-rate-limit case. Deliberately does **not** cover a *confirmed* server-side failure (a real 502/500 response) — retrying that needs a fresh idempotency key or a server-side reset, plus real product decisions the transport-failure fix didn't require (retry limit, automatic vs. a manual "Retry" affordance, which doesn't exist anywhere in the UI today). Tracked below as its own item rather than left implicit.
- **Retry a confirmed server-side import failure** (a real 502/500 response, not just a lost/never-sent request) — needs a design decision first: a fresh `client_import_id` per retry attempt (simplest, creates a new `import_jobs` row) vs. a server-side "reset to processing" RPC, a retry-count cap, and whether it's automatic or needs a manual "Retry" affordance (none exists today on any import screen). See `src/import/api.ts`'s `ImportTransportError` doc comment for why the current fix can't just be extended to this case.
- Add recovery handling for interrupted image uploads.
- ~~Close the orphaned original-photo Storage object gap (T15, Phase 10/ADR-0017).~~ **Done, 2026-08-18** (branch `reliability/orphaned-photo-cleanup`) — see `docs/threat-model.md`'s T15 entry.
- Verify true two-connection concurrency for the invitation-redemption race and import-claim fencing (ADR-0020) — provable by pgTAP for logic, not yet verified under genuine concurrency. See `docs/history/phase-11.5-import-concurrency.md`'s Conditional Pass follow-ups (also `docs/threat-model.md` T18/T19).
- ~~**Enforce the category vocabulary at the schema/decoding level, not just the prompt.**~~ **Investigated and reframed, 2026-08-19** — the premise turned out to be wrong on inspection, not just unimplemented. Two problems found in `server/ai/extractRecipe.ts`'s actual call path (`@anthropic-ai/sdk@0.115.0`'s `zodOutputFormat`/`transformJSONSchema`, `node_modules/@anthropic-ai/sdk/src/lib/transform-json-schema.ts`): (1) this SDK version's structured-output path only forwards a fixed whitelist of JSON Schema keywords (`type`/`properties`/`additionalProperties`/`required`/`items`/`minItems`/a small `format` allowlist/`description`/`title`/`$ref`/`$defs`/`anyOf`/`oneOf`/`allOf`) — `enum` is not in it, so `z.enum(categoryValues)` gets flattened into a `description` string instead of a real constraint, meaning Anthropic's structured-output decoding would never actually see it as a closed set (verified directly: `zodOutputFormat` on a `z.array(z.enum([...]))` schema produces `items: { type: 'string', description: '{enum: [...]}' }`, not a JSON Schema `enum` array). (2) Separately, even if it *did* reach the API, `messages.parse()`'s own client-side validation (`zodObject.safeParse` in the SDK's `zod.ts`) would then throw and fail the *entire* extraction the moment the model emits one category string that isn't an exact case-sensitive vocabulary match — turning today's graceful degradation (`mapCategoryNamesToIds`' fuzzy match silently drops a near-miss, recipe still saves) into a hard import failure, which is a regression for a Reliability-milestone item, not a hardening. Real schema-level enforcement would need either a hand-built JSON Schema bypassing `zodOutputFormat` (unverified whether the API itself supports `enum` at all, independent of this SDK's whitelist — untested, no live call made) or a lenient variant that drops invalid entries instead of failing the parse (its own small design decision). Left here rather than actioned blind — needs a developer look before deciding whether it's worth revisiting, given the closed set is already reasonably enforced by prompt instruction + fuzzy post-hoc mapping (PR #79).

---

## 3. Security & Privacy Readiness

**What done looks like:** known gaps are closed or explicitly accepted-and-documented, *and* the app has had a genuine look for what isn't known yet — not just a checklist of already-found items — before real households outside the dev process have data at stake.

**Backlog:**
- **Full security and privacy audit — a systematic pass, not just closing the already-known items below.** Scoped separately because "fix what's already been found" and "go looking for what hasn't" are different activities with different failure modes if conflated. Covers: a fresh, current-code re-review of the threat model end to end (not spot-checking individual T-entries in isolation); a privacy audit of what data actually gets collected, stored, or leaves the app (recipe content, cooking notes, page text sent to Anthropic, and whatever the Friends & Family Preview telemetry push ends up capturing) against what's actually necessary; a systematic sweep of every RLS policy and Storage bucket, not just the tables a specific bug already pointed at; mobile build-artifact and CI-log review for accidental secret or PII exposure; and a credential-rotation / incident-response dry run against `docs/incident-response.md`, not just trusting the runbook reads well. Best run once the specific known-gap items below are closed, so it's also verifying those fixes rather than racing them.
- SSRF review of the import fetcher against current behavior — a targeted re-check of a specific known mechanism, distinct from the systematic audit above.
- Invitation abuse tests; import cost-abuse tests.
- Dependency review and repository-history secret scan.
- Wire 1Password CLI into CI (Service Account + `op run`) — still not added; concrete consequence is Phase 1's live Claude-extraction test staying `describe.skip`-gated.
- Verify the local SQLite cache's iOS backup inclusion (ADR-0013 "Backup implications") — judged low-severity, but needs real on-device verification of a path-format mismatch before trusting a fix. See `docs/history/phase-06-offline-sync.md`.

---

## 4. Friends & Family Preview

**What done looks like:** real households outside the dev process are using Keepsake, and you can see how.

**Backlog:**
- **Telemetry push — gate, not a normal item.** This has to land before any invite goes out, not sometime during the preview. PostHog's event-allowlist abstraction (ADR-0006) already exists in code but is a no-op today — no real `EXPO_PUBLIC_POSTHOG_KEY` has ever been wired in. Scope: wire the real key, decide and expand the actual event set (what features get used, how often — not just the existing minimal allowlist), and confirm Sentry crash reporting (this milestone's dependency on Reliability's crash-reporting item) is live against a real DSN, not just scaffolded.
- ~~Fix the staging magic-link email.~~ **Done, 2026-08-18** (branch `security/otp-email-template`). Real blocker for an actual new user signing up, not a dev-convenience gap — and turned out to be five compounding bugs, not one (no code-based template for either the magic-link *or* confirmation flow, stale `site_url`, `otp_length` mismatch, and — root cause — no custom SMTP provider configured on this free-tier project, which `ADR-0008` had flagged as a deferred decision back in Phase 3). Fixed by setting up Resend + a verified `keepsake.brightbench.app` sending domain, writing both email templates, and correcting the config drift; verified end-to-end with a real signup. Full account of what was found and fixed — including a real operational gotcha (staging's Auth service needed an explicit restart to pick up config changes) — in `docs/history/cross-cutting-otp-email-fix.md`.
- Real EAS/Xcode build plus a scan of the compiled artifact and build logs — closes the credential-validation journey's remaining half, deliberately scoped here rather than MVP Validation (see that milestone's exception note) since a beta build has to happen for this milestone anyway; scanning it here means one build serving both purposes instead of producing a throwaway one just to close milestone 1 early.
- Beta progression: internal household → trusted households → wider invite-only.

---

## 5. Preview Learning & Iteration

**What done looks like:** you know what's actually working and what isn't, from real usage — not from guessing.

**Backlog:** intentionally empty for now — this milestone's real backlog is what Friends & Family Preview's telemetry and direct feedback surface, not something to pre-guess before that data exists.

---

## Unplaced

- **Swipe-style meal planning.** A major feature you want to build, not yet scoped. Deliberately left with no milestone assignment and no PRD — a separate agent session is writing that PRD. Do not sequence or start implementation work against this until that PRD exists and this entry gets placed for real.

---

## Not yet triaged

Small items with no milestone assignment yet — moved here from `docs/current.md`'s old carried-forward list rather than left with nowhere to live. Needs its own pass to sort into milestones above; not force-sorted into this first draft.

- **`captureFromCamera()` physical-device confirmation** — deliberately deferred by the developer (not forgotten). Low risk: shares its permission/cancel pattern with the already-confirmed `pickExistingPhoto()`.
- **1Password SSH agent guidance** — not addressed. Low priority for a single-developer project.
- **Squash-merge is still allowed at the repo level** — not urgent, not yet revisited.
- **Fake-secret detection fixture** — deliberately not built, trusting gitleaks' maintained ruleset instead.
- **`@bacons/apple-targets` dependency-scan exception** — re-run `npm audit` without `--omit=dev` periodically to check whether the upstream `@xmldom/xmldom` finding gets a fix.
- **Brief onboarding flash on cold launch** — physical-device finding, 2026-08-04: on reload, the "What should we call you?" onboarding screen flashes briefly before correctly landing on This Week for an already-onboarded user. Likely `AuthenticatedRouteBoundary`'s session-then-household loading sequencing. Not urgent — developer's own call to defer.
- **Add/Settings visual redesign** — developer feedback, 2026-08-04: dissatisfied with the current look/feel of the global Add sheet and Settings screen. Developer intends to revisit with a fresh design pass later.
- ~~**ORG-04/AI-06 category mapping is coincidence-reliable, not robust.**~~ **Done, 2026-08-19** (branch `reliability/category-mapping-prompt`) — the extraction prompt now carries the real seeded category list as a closed set instead of letting Claude free-associate a name that had to coincidentally string-match. See `docs/history/phase-08-url-import.md`.
- **Grocery product questions found in the 2026-08-08/09 device-testing round, deliberately not fixed blind:** optional-ingredient phrasing ("white sugar (if needed)"), non-purchasable lines ("reserved pasta water"), and manual staple-marking. See `docs/history/phase-13-grocery-generation.md`.
- **Grocery list merge lands on whichever unit happened to appear first, not the most natural one for the total** — found live testing 2026-08-19 (Journey 1 walkthrough): a plan with crepes (several cups of flour) and cheese sauce (a few tbsp of flour) merged to "66 tbsp flour" instead of something cup-scale. `sumSubgroup()` in `server/groceries/generateGroceryList.ts` anchors the sum on `first.unit` — the arithmetic is correct, the chosen display unit isn't. Deliberately not fixed blind; a real fix needs a target-unit selection rule (e.g. largest/coarsest unit among occurrences) rather than "whichever came first in iteration order."
- **Abbreviated units with a trailing period ("6 oz.") block the parenthetical alternate-unit strip, and the affected recipe predates the fix anyway** — found live testing 2026-08-19 (Journey 1 walkthrough): "5 Minute Nacho Cheese Sauce" (created 2026-08-07) doubled from "6 oz (180g) medium cheddar cheese" to "12 oz (180g)" — the primary quantity scaled, the parenthetical didn't. Two compounding causes: (1) the recipe was created before the Aug 15 parenthetical-stripping fix (`4b79a3d`) and wasn't part of that fix's 20-row backfill, and (2) `stripParentheticalAlternateUnit()`'s `OPEN_PAREN` check (`server/units/parseQuantity.ts:195`, anchored `/^\(\s*/`) still wouldn't catch this pattern live today — `matchUnit` consumes "oz" but not the source text's trailing period, so the remaining string starts with "." instead of "(" and the anchored regex never matches. Not just a missed backfill row; a live gap for any unit written with an abbreviating period. Deliberately not fixed blind.
- **Cooking Mode's "Done Cooking" button needs two taps after typing a note** — found live testing 2026-08-19 (Journey 1 walkthrough): first tap only dismisses the keyboard, second tap actually completes (clears checklist, records timestamp, saves note). Root cause: `Sheet.tsx:156`'s `KeyboardAvoidingView behavior="padding"` (added in `d900c65`) repositions the sheet — including the confirm button — live as the keyboard animates closed, so the first tap's touch-up lands outside the button's shifted bounds and RN treats the gesture as cancelled. Likely fix direction: blur the note `TextInput` explicitly on press before the layout shifts, or swap to `behavior="height"`/a fixed `keyboardVerticalOffset` so the button doesn't move mid-gesture. Deliberately not fixed blind.
- **A permanently-deleted recipe can leave a zombie cooking-event outbox item** on another device that had an unsynced completion for it — narrow window, bounded to log noise/wasted retries. Real fix needs a terminal/no-op outcome added to the outbox's retry classification (an ADR-0024 design change). See `docs/history/phase-16-archive-delete-lifecycle.md`.
- **Phase 9 — no physical-device pass for the Share Extension confirmation UI.** ADR-0003 requires one specifically for this phase (Simulator isn't fully representative of Share Extension behavior); unconfirmed: the 1.5s+checkmark+haptic confirmation animation. `IMP-02` stays `Done (untested)` in `docs/prd-traceability.md` until this closes. See `docs/history/phase-09-share-extension.md`.
- **Phase 10 — no live Anthropic vision call yet.** Photo-import extraction has Jest evidence only; needs a real `extractRecipeFromImage` call against a real photo, with the developer watching (per this project's live-API-test convention). See `docs/history/phase-10-camera-photo-import.md`.
- **Phase 15 — no physical-device pass yet (ADR-0003).** Developer decision, 2026-08-11: deliberately deferred past Phase 16 rather than blocking on it then — do it in a later live-testing round. `COOK-*`/`NOTE-*`/`REC-05`/`OFF-03`/`OFF-05` stay `Done (tested)Δ` in `docs/prd-traceability.md` until this closes. See `docs/history/phase-15-cooking-mode.md`.
