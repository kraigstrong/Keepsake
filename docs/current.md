# Current Status

The single source of truth for "where are we." Update this at the start and end of every session — this is a pointer, not a log. Full phase-by-phase history (design decisions, bugs found, exact commit/test counts) lives in `docs/history/*.md`, one file per phase — read a specific one only when you need that phase's detail, not as part of routine session startup.

Phase 0–11.5's history entries were migrated from the old, single `phase-status.md` largely as-written — some restate reasoning that already lives in their own ADR. Starting with Phase 12, keep new entries short: what shipped, what broke and got fixed, the evidence numbers — link to the ADR for *why* rather than re-narrating it (see `CLAUDE.md`'s "Comments and phase records" rule).

## Current

- **Phase:** 17 — End-to-End Product and Security Validation. In progress. Phase 16 formally exited Pass, 2026-08-13 (see History table and `docs/history/phase-16-archive-delete-lifecycle.md`) — folded in both PR #50's walkthrough round and the ADR-0026 cross-cutting fix's evidence, per the plan noted at Phase 16's build-scope completion. Phases 12–16 have all formally exited (12: Conditional Pass, 2026-08-07; 13/14: Pass, 2026-08-10; 15: Conditional Pass, 2026-08-11; 16: Pass, 2026-08-13 — see History table). Codex's review on PR #53 caught a real gap in that Pass: Phase 16's lifecycle RPCs have no auditable actor (unlike this app's `added_by`/`cooked_by` precedent from Phases 12/15) — developer decision, 2026-08-13: fix now rather than downgrade or defer. `archived_by`/`deleted_by`/`restored_by` columns were written and pushed to staging the same day, but on a separate exit-decision branch (`docs/phase-16-exit-decision`) that never got merged forward into the Phase 17 branches — this doc's own claim below that lifecycle was "re-verified after the migration landed" was true of staging but not of this branch's git history. Caught by Codex review, PR #54; folded into this branch 2026-08-16.
- **Status:** Phase 17 has no build scope of its own — it's validation of the complete MVP as one coherent product, via the six required journeys in execution-plan.md §"Phase 17" (website success, manual/offline, shared household, lifecycle, security, credential validation). ADR-0003 requires at least one full pass through those journeys on a physical device, in addition to Simulator coverage. Traceability sweep done 2026-08-13 — three stale entries corrected with real evidence (LIB-01/LIB-02: fully implemented in `librarySort.ts`/`sortPreference.ts`, just never updated from Phase 7's initial "In Progress"; SEC-06: SSRF-hardened fetch + zod-validated AI extraction + validated deep links, never updated from Phase 8; OFF-04: proactive offline-gating on This Week/Grocery Review plus reactive error handling on Import/Recipe Editor, no local-write path exists per ADR-0013). Security journey (Household A/B, non-member reads/writes denied, sign-out clears cache) confirmed already comprehensively covered: dedicated `household_isolation.test.sql`/`recipe_isolation.test.sql` plus per-RPC cross-household-rejection cases in nearly every phase's own pgTAP suite (all running against real Postgres/RLS in CI, not mocked), `wipeOfflineData` confirmed wired into `SessionProvider`'s sign-out path. Credential-validation journey partially run: generated the iOS native project locally (`expo prebuild --platform ios --no-install`) and scanned it with `gitleaks detect --no-git` — clean, no secret names or values embedded — then removed the generated directory (gitignored scratch artifact, not committed).
- **Next action — six required journeys, current coverage:**
  1. **Website success** (create household → import → find → plan → servings → groceries → export → cook → note): **Simulator walkthrough attempted, blocked partway.** Confirmed working live: Library browsing, Recipe Detail, servings scaling (2× tested against two different recipes, both correct). Blocked at "add this recipe to This Week" — see the tap-target finding below. Grocery generation, export, Cooking Mode, and note-adding within this journey remain unexercised as a result. The URL-import leg was deliberately not attempted (this project's live-Anthropic-API convention — needs the developer watching, see Blocked on).
  2. **Manual and offline** (create → sync → offline → find/open/cook → complete offline → reconnect → verify exactly one event): not attempted this round (blocked before reaching it — see below). OFF-01/02/03/05 are `Done (tested)` individually, and `record_cooking_event`'s idempotency (client-generated `client_event_id`, `ON CONFLICT`) already proves no duplicate event on reconnect/retry, so the automated case for this journey is still strong even without a fresh walkthrough.
  3. **Shared household** (A creates → B joins → A imports → B plans → A edits → B hits a conflict): well covered piecewise (`invitation_*.test.sql`, WEEK-07 sync footnote, Phase 5's conflict UI), but this is inherently a two-actor scenario pgTAP's single-transaction model can't fully replicate — same limitation already flagged for Phase 11.5's concurrency gap. Needs an actual two-session manual walkthrough.
  4. **Lifecycle** (archive → restore → delete → restore → permanently delete): **confirmed working live in Simulator**, full cycle — archive, unarchive, delete, restore, delete again, permanently delete — ending correctly on "Recently Deleted is empty." This was re-verified against staging after the audit-actor migration was pushed there, on a manually-created test recipe, cleaned up by the journey's own permanent-delete step. (The migration's *branch history* lagged staging until 2026-08-16 — see the Phase bullet above.)
  5. **Security** (non-member reads/writes denied, sign-out clears cache): the best-covered journey already — see Status above. A single manual confirmation (one real cross-household REST attempt, denied) would be a nice-to-have close, not a gap.
  6. **Credential validation** (scan bundles, generated native projects, build logs): both native-project scans done clean this session — iOS (`expo prebuild --platform ios --no-install` + `gitleaks detect --no-git`, no secret names/values embedded) and Android (same pattern, `expo prebuild --platform android --no-install` — incidentally wrote an `android.package` field into `app.json`, reverted since a permanent package name is a real product decision, not a side effect to slip in from a scan). Both generated directories removed after scanning (gitignored scratch artifacts, never committed). Still needed: a scan of an actual compiled `.app`/`.ipa`/`.aab` and real build logs — requires a full EAS/Xcode build with the developer's own build credentials, not something this session can produce.

- **Simulator finding (unresolved, needs developer verification): full-width primary action buttons don't respond to automated taps.** "Add to This Week" (Recipe Detail) and "Next" (the Add Recipes picker) never registered a tap across two independent sessions, many coordinate/timing variations, and a full app reload — while every other control on the same screens (checkboxes, the tab bar, back navigation, the "Cancel" link, small secondary buttons like Archive/Delete) worked correctly and consistently. Source inspection found nothing structurally unusual — both are the same shared `Button`/`Pressable` component (`src/components/Button.tsx`) every other working button also uses. Not committed as a confirmed app bug: it may be a Simulator-automation-tool limitation specific to this button's bottom-safe-area-inset layout, not something a real finger/mouse would hit. **Needs a real tap test** (by the developer, or via the physical-device pass this phase already requires) to determine whether this is a genuine defect or an automation-tool artifact before it's actioned either way.
- **Blocked on:** Everything left is the developer's own hands-on pass — same pattern every prior phase used (build automated coverage, then a real walkthrough finds and fixes what's left), not something this session can push further alone. One consolidated session should cover it: (1) resolve the tap-target finding with a real tap; (2) once resolved, finish journey 1 (grocery generation → export → Cooking Mode → note) and the live URL-import leg (needs the developer watching, per this project's live-Anthropic-API convention); (3) journey 2 (offline/reconnect, verify exactly one event); (4) journey 3 (shared household, needs two sessions/devices at once); (5) journey 6's remaining half whenever a real EAS/Xcode build is next produced; (6) the ADR-0003 physical-device pass this phase requires regardless, ideally combined with (1)-(4) rather than a separate round.

## History

| Phase | Result | Date | Summary |
|---|---|---|---|
| — | — | 2026-07-31 | Repository scaffolded. No product phase started yet. |
| [0](history/phase-00-baseline.md) | Pass | 2026-08-01 | Product Baseline and Quality Harness — Expo/TS scaffold, CI (4 required checks), threat model/runbook/release checklist. |
| [1](history/phase-01-native-risk-spikes.md) | Pass | 2026-08-02 | Native Feasibility and Risk Spikes — 9 risk spikes, each device-verified. |
| [2](history/phase-02-application-shell.md) | Pass | 2026-08-02 | Application Shell and Design Foundation — Expo Router, auth boundary, design tokens/primitives. |
| [3](history/phase-03-household-auth.md) | Conditional Pass | 2026-08-02 | Authentication, Household, and Security Boundary — email OTP, household/invitation RPCs + RLS. |
| [3.5](history/phase-03.5-design-direction.md) | Pass | 2026-08-02 | Design Direction and Visual Iteration — "Ink & Paper" visual pass. |
| [4](history/phase-04-manual-recipe.md) | Conditional Pass | 2026-08-03 | Manual Recipe Vertical Slice — recipe schema/RLS, `save_recipe`, hero image, editor/detail screens. |
| [5](history/phase-05-drafts-versions-conflicts.md) | Conditional Pass | 2026-08-04 | Drafts, Version History, and Edit Conflicts — versioning, drafts, restore, conflict UI. |
| [5.5](history/phase-05.5-optional-password-signin.md) | — (no gate) | 2026-08-04 | Optional Password Sign-in (ADR-0012) — opt-in password alongside OTP. |
| [6](history/phase-06-offline-sync.md) | Pass | 2026-08-03 | Offline Read Model and Synchronization — local SQLite mirror, sync engine, image cache. |
| [7](history/phase-07-library-search.md) | Conditional Pass | 2026-08-04 | Library, Smart Sort, Search, and Filters — FTS5 search, sort, filters. |
| [8](history/phase-08-url-import.md) | Pass | 2026-08-05 | URL Import Foundation — SSRF-hardened fetch, Claude extraction, rate limits. |
| [9](history/phase-09-share-extension.md) | Conditional Pass | 2026-08-05 | Safari Share Sheet and Bulk URL Import — Share Extension, durable outbox, bulk paste. |
| [10](history/phase-10-camera-photo-import.md) | Pass | 2026-08-05 | Camera and Existing Photo Import — vision extraction, preserved original photo. |
| [JSON-LD hint](history/cross-cutting-jsonld-hint.md) | Merged | 2026-08-06 | JSON-LD structured-data import hint (ADR-0019) — cross-cutting, not a numbered phase. |
| [11](history/phase-11-units-scaling.md) | Conditional Pass | 2026-08-05/06 | Units, Scaling, and Quantity Integrity — structured quantities, conversion, scaling UI. |
| [11.5](history/phase-11.5-import-concurrency.md) | Conditional Pass | 2026-08-06/07 | Import Concurrency and Local Data Isolation (ADR-0020) — invitation race, import fencing, household-scoped local data. |
| [12](history/phase-12-this-week-planning.md) | Conditional Pass | 2026-08-07 | This Week Planning (ADR-0021) — weekly plan schema/RLS/RPCs, multi-select add, tap-based reorder, Frequently Selected sort. |
| [13](history/phase-13-grocery-generation.md) | Pass | 2026-08-10 | Grocery Generation and Review (ADR-0022) — canonical identity, conservative merging, static categories/staples, grouped review. |
| [14](history/phase-14-reminders-export.md) | Pass | 2026-08-10 | Apple Reminders Export (ADR-0023) — point-of-use permission, duplicate protection, local-only export bookkeeping. |
| [15](history/phase-15-cooking-mode.md) | Conditional Pass | 2026-08-10/11 | Cooking Mode and Cooking History (ADR-0024) — checklist, scaling, keep-awake, Done Cooking, offline outbox, cooking history. |
| [ADR-0026 multiplier](history/cross-cutting-adr-0026-multiplier.md) | Merged | 2026-08-12 | Multiplier as the canonical recipe-scaling unit ([PR #51](https://github.com/kraigstrong/Keepsake/pull/51)) — replaces `planning_entries.servings`, removes PR #50's assumed-base stopgap everywhere it existed. Migrations pushed to staging. |
| [16](history/phase-16-archive-delete-lifecycle.md) | Pass | 2026-08-13 | Archive, Recently Deleted, and Destructive Lifecycle (ADR-0025) — archive/unarchive, soft delete, restore, permanent delete with client-side asset cleanup, server-enforced exclusion from planning. [PR #49](https://github.com/kraigstrong/Keepsake/pull/49), [PR #50](https://github.com/kraigstrong/Keepsake/pull/50). |

## Open Conditional Pass follow-ups

Every phase's Conditional-Pass evidence gaps are recorded in that phase's own `docs/history/*.md` file, but a gap only gets *listed here* while it's still genuinely unresolved — the point of this section is that a session reading only `docs/current.md` shouldn't need to open per-phase archives to find outstanding required evidence. When one closes, move it to the relevant history file's own follow-ups section (mark it resolved there) and delete it from here rather than leaving a stale copy in both places.

- **Phase 9 — no physical-device pass for the Share Extension confirmation UI.** ADR-0003 requires one specifically for this phase (Simulator isn't fully representative of Share Extension behavior). Unconfirmed: the 1.5s+checkmark+haptic confirmation animation. `IMP-02` stays `Done (untested)` in `prd-traceability.md` until this closes. See `docs/history/phase-09-share-extension.md`.
- **Phase 10 — no live Anthropic vision call yet.** Photo-import extraction has Jest evidence only; needs a real `extractRecipeFromImage` call against a real photo, with the developer watching (per this project's live-API-test convention). See `docs/history/phase-10-camera-photo-import.md`.
- **Phase 11.5 — true two-connection concurrency unverified.** The invitation-redemption race (KS-002) and import-claim fencing (KS-004) fixes are provable by pgTAP for their logic, but not for genuine two-connection concurrency, which pgTAP's single-transaction model can't express. Needs a manual staging check (two `psql` sessions racing the same call) before threat-model T18/T19 are fully closed. See `docs/history/phase-11.5-import-concurrency.md`.
- **Phase 15 — no physical-device pass yet (ADR-0003).** Required specifically for this phase (screen-awake/real-kitchen-use class, same as Phase 14). Developer decision, 2026-08-11: deliberately deferred past Phase 16 rather than blocking on it now — do it in a later live-testing round. `COOK-*`/`NOTE-*`/`REC-05`/`OFF-03`/`OFF-05` stay `Done (tested)Δ` in `prd-traceability.md` until this closes. See `docs/history/phase-15-cooking-mode.md`.

Older phases (4, 5, 7, 11) also exited Conditional Pass on a generic "no live Simulator demonstration" gap, never individually marked resolved. Not listed here as still-open: by Phase 6 onward, live Simulator/device sessions became routine and have exercised most of the same UI surfaces since, without a phase going back to formally close the earlier note. Treated as superseded in practice, not silently dropped — flagged here so that judgment call is visible rather than assumed.

## Carried-forward items (not phase-blocking, but tracked so they aren't lost)

- **`captureFromCamera()` physical-device confirmation** — deliberately deferred by the developer (not forgotten). Low risk: shares its permission/cancel pattern with the already-confirmed `pickExistingPhoto()`.
- **1Password CLI wiring into CI** (Service Account + `op run`) — still not added. Concrete consequence: Phase 1's live Claude-extraction test is `describe.skip`-gated on `ANTHROPIC_API_KEY`, which is never present in CI.
- **1Password SSH agent guidance** — not addressed. Low priority for a single-developer project.
- **Squash-merge is still allowed at the repo level** — not urgent, not yet revisited.
- **Fake-secret detection fixture** — deliberately not built, trusting gitleaks' maintained ruleset instead.
- **`@bacons/apple-targets` dependency-scan exception** — re-run `npm audit` without `--omit=dev` periodically to check whether the upstream `@xmldom/xmldom` finding gets a fix.
- **Local SQLite cache is included in iOS device/iCloud backups** — found in Phase 6 (ADR-0013's "Backup implications" section). `expo-sqlite`'s default location is the backed-up Documents-directory-equivalent, not the (correctly-excluded) cache directory hero images use. Judged low-severity (a rebuildable server mirror, no credentials) and not fixed blind — needs on-device verification of a path-format mismatch before trusting a `Paths.cache` redirect. Still not addressed as of Phase 11.5.
- **Sentry/PostHog accounts** — not yet created; both stay no-ops until the developer adds real `EXPO_PUBLIC_SENTRY_DSN`/`EXPO_PUBLIC_POSTHOG_KEY` values to the `Keepsake Client` 1Password Environment.
- **Brief onboarding flash on cold launch** — physical-device finding, 2026-08-04: on reload, the "What should we call you?" onboarding screen flashes briefly before correctly landing on This Week for an already-onboarded user. Likely `AuthenticatedRouteBoundary`'s session-then-household loading sequencing. Not urgent — developer's own call to defer. Best fit is probably Phase 18 (Performance, Accessibility, Privacy, Security, and Reliability Hardening).
- **Add/Settings visual redesign** — developer feedback, 2026-08-04: dissatisfied with the current look/feel of the global Add sheet and Settings screen. Developer intends to revisit with a fresh design pass later; no specific phase assigned yet.
- **ORG-04/AI-06 category mapping is coincidence-reliable, not robust** — found at Phase 8 exit review. Import's category assignment is exact-string matching Claude's free-text guess against an 11-value seeded vocabulary Claude is never told about. Worth passing the real category list into the extraction prompt. No phase assigned yet.
- **Staging magic-link email is unusable for real sign-up** — found 2026-08-03. `signInWithOtp` sends Supabase's *default* magic-link template, which only renders a clickable link, not the `{{ .Token }}` code `verifyOtp` expects the user to type. Not blocking phase-by-phase dev testing (bootstrap a test account by hand via Dashboard, then use password sign-in), but a real blocker for any actual new user signing up before launch. Fix: add a custom `magic_link` email template (`supabase/templates/`) surfacing `{{ .Token }}`, and push it to staging.
- **Orphaned original-photo Storage objects (T15, Phase 10, ADR-0017)** — upload-before-processing means a photo upload that succeeds but whose extraction/save fails afterward (or whose client abandons the flow mid-way) leaves an unreferenced object in the `recipe-images` bucket. Not a data-exposure risk, just an accumulating storage-cost item. No cleanup mechanism built yet — Phase 9's 30-day outbox expiry is a plausible pattern to reuse if this becomes a real cost concern in practice.
- **True two-connection concurrency verification (Phase 11.5, ADR-0020)** — the invitation-redemption race (KS-002) and import-claim fencing (KS-004) fixes are provable by pgTAP for their logic, but not for genuine two-connection concurrency, which pgTAP's single-transaction model can't express. Worth a manual staging check (two `psql` sessions racing the same call) before considering threat-model T18/T19 fully closed.
- **Phase 13/14 product questions found in the 2026-08-08/09 device-testing round, deliberately not fixed blind:** optional-ingredient phrasing ("white sugar (if needed)"), non-purchasable lines ("reserved pasta water"), and manual staple-marking — likely a later phase. See `docs/history/phase-13-grocery-generation.md`.
- **Phase 14 — no un-export mechanism.** Excluding an already-exported item in review doesn't retract it from Reminders (ADR-0023 decision 4, deliberate). Revisit only if a real user need surfaces. See `docs/history/phase-14-reminders-export.md`.
- **Phase 14 — native-"Groceries"-list detection is a title match only.** EventKit has no API to detect Apple's special auto-categorizing list type, so a coincidentally-named ordinary list is used too — harmless (items just won't auto-sort). See `docs/history/phase-14-reminders-export.md`.
- **Phase 15 — no length cap on `cooking_events.note`**, client or server, unlike every other free-text column in this schema. Low severity for this app's friends/family scale; close before/alongside any future work touching the note-capture path. See `docs/history/phase-15-cooking-mode.md`.
- **Phase 15 — Cooking Mode doesn't offer an arbitrary (stepper) servings count**, unlike Recipe Detail's own scaling controls. Partially resolved 2026-08-12: the scale now defaults to the This-Week plan's servings count when this recipe is on the current confirmed plan. Resolved 2026-08-14 (Phase 17 walkthrough): Cooking Mode now fetches and applies the household's preferred unit system the same way Recipe Detail does (found live — a recipe whose source mixed lb/g per-ingredient displayed that same mix scaled up, "2400g tomato, 1lb beef," reading as inconsistent even though each line was individually correct). No toggle in Cooking Mode, unlike Recipe Detail — defaults straight to preferred, matching its single-scrolling-screen minimalism; the complaint was inconsistency; not wanting a choice. Only the arbitrary-count-stepper gap remains open — revisit if it becomes a real complaint too.
- **Phase 15 — Reset checklist has no confirmation or undo**, unlike this app's own precedent (This Week's remove has Undo). Revisit alongside any other Cooking Mode UX pass.
- **Phase 15 — "Start Cooking" vs. "Add to This Week" button emphasis on Recipe Detail** — an unconfirmed UI-emphasis call (Start Cooking made primary); revisit if usage or developer feedback suggests the wrong one is primary.
- **Phase 15 — `submitPendingCookingEvents` call inside `CookingModeScreen`'s Done Cooking handler skips the account-switch-guard callback** `app/_layout.tsx`'s own wiring uses (ADR-0020's TOCTOU fix). Very low practical risk; close when convenient.
- **Phase 16 — a permanently-deleted recipe can leave a zombie cooking-event outbox item.** An unsynced Phase 15 cooking-completion event for a recipe that gets `permanently_delete_recipe`d before it syncs retries forever (no terminal-failure state in `submitPendingCookingEvents`, by ADR-0024's own design) against a recipe that's actually gone, logging an error every foreground/reconnect drain. Narrow trigger window, bounded to log noise/wasted calls — a real fix needs a terminal/no-op outcome added to the outbox's retry classification (an ADR-0024 design change), not a contained patch. Found by Codex review, PR #49; see ADR-0025's 2026-08-12 amendment.
- **Phase 16 — Archive/Delete buttons on Recipe Detail read as visually disconnected**, just sitting in the plain action row (ADR-0025 decision 7's own deviation from an overflow menu). Developer feedback from the first real walkthrough, 2026-08-12: functionally correct, but the UX "didn't feel right" — floated a trash-icon-first, archive-or-delete-after pattern as a possible direction. Explicitly deferred by the developer, not scoped or designed yet; revisit alongside any future overflow-menu-component work (see decision 7's "alternatives considered").
- **Phase 17 walkthrough — 79 of 139 `recipe_ingredients` rows (57%) on staging had never been parsed** (`ingredient_text`/`quantity_min`/`unit` all `null`), found while root-causing why "large eggs" and "large eggs, at room temperature" wouldn't merge on the grocery list. Every recipe created on 2026-08-05 was 100% unparsed; everything from 2026-08-07 onward (when Phase 11's parsing pipeline actually landed) was mostly or fully parsed — the five pre-Phase-11 recipes' ingredient rows were simply never backfilled once parsing shipped. One-time backfill run directly against staging, 2026-08-14 (developer approved): re-ran `parseQuantity()` — pure syntax, no AI call — against all 79 null rows and updated the 70 that now parse cleanly; 9 stayed `null` correctly (genuinely quantity-less lines like "Salt and pepper, to taste," "Butter, for coating the pan"). No code changed — this was a data-only fix for historical rows; new imports have gone through the real parser since Phase 11.
- **Phase 17 walkthrough — alternate-unit annotations ("1 lb / 500g beef," "800g, 28oz can crushed tomato," "(290 g) all-purpose flour") didn't scale, and the parenthesized form was also silently blocking "all purpose flour" from folding into "flour."** Only the leading quantity is ever structured (ADR-0018 — one quantity/unit per line); a second, alternate-unit number sitting inertly inside `ingredientText` doesn't scale with it ("3 lb / 500g beef" after 3x is visibly wrong), and when it sat *before* the ingredient name (the parenthesized form), it blocked `canonicalKey()`'s `DEFAULT_VARIETY_PREFIXES` fold from ever reaching "all-purpose" — which also silently broke staple detection for the same ingredient, since `isStaple()` checks the same canonical key. Fixed in `parseQuantity.ts` (`stripAlternateUnit`/`stripParentheticalAlternateUnit`): stripped at parse time rather than parsed and scaled — developer decision, matches the "don't show a number this code can't vouch for" posture ADR-0018 already takes, and Original/Preferred unit display (UNIT-02) already covers seeing a converted amount that stays in sync with scaling. `import-recipe` redeployed twice (`parseQuantity.ts` is bundled into it) so new imports get both fixes. Backfilled 20 already-parsed-but-still-noisy rows across staging (3 slash/comma, 17 parenthetical) — the earlier 79-row null-parse backfill didn't touch these since they already had non-null `quantity_min`. Confirmed fixed: `canonicalKey('flour') === canonicalKey('all-purpose flour')` and `isStaple()` agrees on both.
- **Phase 17 walkthrough — the grocery list had the same "whichever unit the source wrote first" problem as Cooking Mode, and stripping the alternate unit (previous item) made it worse.** Once only one unit survived parsing, the grocery list showed exactly that unit with no awareness of the household's own preference ("2400g tomato, 1lb beef" on the list itself, not just in Cooking Mode). Developer wanted the surviving unit to default to whichever the household prefers when a recipe originally listed both ("800g/28oz crushed tomato" should show as 28oz, not 800g). Fixed by threading the same `fetchProfile` → `preferredUnitSystem` → `convertToSystem()` flow already shipped for Cooking Mode through `generateGroceryList()`/`fetchGroceryReview()`/`GroceryReviewScreen.tsx`, converting each occurrence before grouping (unit class survives conversion, so summability is unaffected — only which unit a total lands in). Developer explicitly declined a packaging-size special case (e.g. always favoring "28oz" as a recognizable can size over whatever `bestFitUnit` computes) — chose "keep it consistent" with Cooking Mode/Recipe Detail's existing best-fit behavior. No Edge Function redeploy needed (grocery generation is client-side only).
- **Two rarer alternate-unit phrasings found while inspecting the backfill, deliberately not chased further:** `"3 cup(s) all-purpose flour"` (an optional-plural `"(s)"` directly suffixed to the unit word, not a number — `stripParentheticalAlternateUnit` correctly declines to touch it, leaving `"(s) all-purpose flour"`) and `"(600 ml / 290 g) all-purpose flour"` (two alternate units nested inside one parenthetical — the strip expects a single number+unit before the closing paren, so a `/`-separated pair inside stops it early, leaving the whole parenthetical). Neither was part of what was reported; both are narrower, rarer variants of the same underlying pattern. Revisit only if they turn out to be common enough to matter — same "accept some gap, escalate deliberately" posture this app already takes elsewhere (ADR-0018/ADR-0022), not something to keep patching blind.
- **Codex review, PR #54 — three findings, 2026-08-16.** (1) LIB-01's 2-week cutoff was inclusive at the exact boundary (`>= cutoff`), one day past what "\<2wk" (strict) requires — fixed to `> cutoff` (developer decision: match the literal requirement rather than keep the inclusive interpretation), `librarySort.test.ts` now covers both sides of the boundary. (2) The Phase 16 audit-actor migration (`archived_by`/`deleted_by`/`restored_by`) was real and already pushed to staging, but stranded on `docs/phase-16-exit-decision` — never merged into any Phase 17 branch, so this doc's own "re-verified after the migration landed" claim didn't match this branch's history. Folded forward, same commits (`git cherry-pick`), no new migration written. (3) **New finding, not previously covered:** photo-import uploads declare their own `contentType` to Supabase Storage and the bucket's MIME allowlist trusts that declaration, not the actual bytes — `import-recipe` then sends whatever was uploaded to Anthropic as `image/jpeg` unsniffed. RLS still scopes the blast radius to the uploader's own household (wasted Anthropic tokens against their own quota, not cross-household exposure), so accepted as a low-severity gap rather than fixed blind — tracked as `docs/threat-model.md`'s T23. Worth a real fix (magic-byte sniff or re-encode-and-validate in `import-recipe/index.ts`) before this app's audience grows past friends/family.

## Open questions for the developer

None currently open.
